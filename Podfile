# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SDKIntegrationTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod "CleverTap-iOS-SDK"
  pod 'FirebaseCore'
  pod 'FirebaseMessaging'
  
  # Pods for SDKIntegrationTest

  target 'SDKIntegrationTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SDKIntegrationTestUITests' do
    # Pods for testing
  end

end
